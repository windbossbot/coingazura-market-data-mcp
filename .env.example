# MCP local stdio mode does not require env vars today.
# Keep future secrets here if hosted/authenticated mode is added.

# Optional future hosted mode example
# MCP_HOSTED_BASE_URL=
# MCP_AUTH_TOKEN=

# Optional future observability example
# MCP_LOG_LEVEL=info
